# Plinth POC — Implementation Plan

> **Scope:** Data platform and ingestion pipeline (breadth-first). Report generation is a subsequent phase.
> **Environment:** Single dev machine, Docker Compose, local network only. No cloud. No prod separation during POC.
> **Geographic start:** Northeastern Oklahoma. Expand regionally/nationally in later phases.
> **Stack:** Python (uv) · FastAPI · PostgreSQL 15 + PostGIS · MinIO · Prefect · Mapbox

---

## Hardware Recommendation

The full national dataset for Phase 1 is 500 GB–2 TB of raw data (DEMs, FEMA NFHL, SSURGO, NLCD, NHDPlus HR). Plan for it even though POC starts regionally.

| | Minimum (POC only) | Recommended (grows to national) |
|---|---|---|
| **CPU** | 8-core | 12–16 core |
| **RAM** | 32 GB | 64 GB |
| **SSD (OS + Postgres + Docker)** | 512 GB NVMe | 1 TB NVMe |
| **HDD (MinIO raster tiles)** | 2 TB | 4–8 TB |
| **Approx. cost (used server)** | ~$400–500 | ~$600–900 |

**Best value: used Dell PowerEdge R730 or HPE DL380 Gen9** on eBay — enterprise DDR4, dual-socket Xeon, hot-swap bays, easily upgradeable. Expect $500–800 for a 64 GB / 12-core / 4 TB config.

> PostgreSQL + PostGIS needs 8–16 GB RAM headroom for spatial index builds. GDAL/rasterio workers use 4–8 GB per concurrent process. Prefect, MinIO, and app containers add ~4 GB. 32 GB is workable for regional; 64 GB is comfortable for national parallel ingestion.

---

## Repository Structure

```
plinth/
├── docker-compose.yml
├── docker-compose.override.yml        # local dev overrides
├── .env.example
├── pyproject.toml                     # uv-managed, defines plinth-cli
├── uv.lock
├── Dockerfile.worker                  # ingestion + report workers
├── docs/
│   └── plans/
├── plinth/
│   ├── __init__.py
│   ├── cli/
│   │   └── __init__.py                # plinth-cli entry point (click)
│   ├── ingest/
│   │   ├── __init__.py
│   │   ├── base.py                    # BaseIngestor class
│   │   ├── fema_nfhl.py
│   │   ├── census_tiger.py
│   │   ├── usgs_3dep.py
│   │   ├── nlcd.py
│   │   ├── ssurgo.py
│   │   ├── nhd.py
│   │   ├── iecc.py
│   │   ├── fema_nri.py
│   │   ├── usgs_seismic.py
│   │   ├── usda_whp.py
│   │   ├── noaa_normals.py
│   │   └── api/
│   │       ├── epa_aqs.py
│   │       ├── nasa_power.py
│   │       ├── usgs_earthquakes.py
│   │       └── fcc_broadband.py
│   ├── flows/                         # Prefect flows
│   │   ├── ingest_all.py
│   │   ├── refresh_fema.py
│   │   ├── refresh_annual.py
│   │   └── check_staleness.py
│   ├── db/
│   │   ├── __init__.py
│   │   ├── connection.py
│   │   └── migrations/                # SQL migration files (numbered)
│   ├── raster/
│   │   ├── __init__.py
│   │   ├── cog.py                     # COG conversion helpers
│   │   ├── clip.py                    # Bounding-box clip + buffer
│   │   └── minio.py                   # MinIO upload/download helpers
│   └── config.py                      # Settings (pydantic-settings)
├── tests/
│   ├── conftest.py
│   ├── test_ingest/
│   └── test_raster/
└── scripts/
    └── bootstrap.sh                   # One-shot env setup
```

---

## Phase 0 — Infrastructure Setup

**Goal:** Running Docker Compose stack, empty but healthy databases, CLI scaffold.

### 0.1 Docker Compose Stack

Services:

| Service | Image | Purpose |
|---|---|---|
| `postgres` | `postgis/postgis:15-3.4` | Primary DB — spatial data, job queue, registry |
| `minio` | `minio/minio:latest` | Local object storage for raster COG tiles |
| `prefect-server` | `prefecthq/prefect:3-latest` | Orchestration UI + API |
| `prefect-worker` | Custom (`Dockerfile.worker`) | Runs ingestion flows |
| `api` | Custom (later) | FastAPI — report requests, report generation |

All services share a Docker network (`plinth-net`). Volumes:
- `postgres-data` → `/var/lib/postgresql/data`
- `minio-data` → `/data` (mount the large HDD path here)

### 0.2 Environment Variables (`.env`)

```
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=plinth
POSTGRES_USER=plinth
POSTGRES_PASSWORD=...

MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=...
MINIO_SECRET_KEY=...
MINIO_BUCKET_RASTERS=plinth-rasters

MAPBOX_TOKEN=...
PREFECT_API_URL=http://prefect-server:4200/api

# Regional bounds for ingestion scope (expand later)
REGION_BBOX_MINX=-96.5
REGION_BBOX_MINY=35.5
REGION_BBOX_MAXX=-94.5
REGION_BBOX_MAXY=37.0
```

### 0.3 CLI Scaffold

`plinth-cli` is a Click-based CLI installed from the package:

```bash
# Developer workflow
uv run plinth-cli ingest fema-nfhl --region ne-oklahoma
uv run plinth-cli ingest all --region ne-oklahoma
uv run plinth-cli db migrate
uv run plinth-cli db status          # shows data_source_registry
uv run plinth-cli raster index       # rebuilds raster_tiles index
```

### 0.4 Deliverables — Phase 0

- [ ] `docker-compose.yml` with all services healthy
- [ ] `bootstrap.sh` — installs uv, builds containers, runs migrations
- [ ] `plinth-cli` installed and callable from worker container
- [ ] PostGIS `plinth` database created with extensions
- [ ] MinIO bucket created
- [ ] Prefect server reachable at `http://<host>:4200`

---

## Phase 1 — Foundation Database Schema

Before any data is loaded, the schema scaffolding must exist.

### 1.1 Core Registry & Infrastructure Tables

```sql
-- Every data source. Populated by ingestor code on first run.
CREATE TABLE data_source_registry (
    source_name          TEXT PRIMARY KEY,
    dataset_version      TEXT,
    last_downloaded      TIMESTAMPTZ,
    update_frequency     TEXT,          -- 'monthly', 'annual', 'decadal', etc.
    next_review_date     DATE,
    coverage_region      TEXT,          -- 'ne-oklahoma', 'oklahoma', 'national'
    notes                TEXT
);

-- Spatial index of raster tiles stored in MinIO.
-- One row per COG tile file.
CREATE TABLE raster_tiles (
    id              SERIAL PRIMARY KEY,
    dataset         TEXT NOT NULL,      -- 'usgs-3dep', 'nlcd', 'usgs-seismic', etc.
    s3_key          TEXT NOT NULL UNIQUE,
    bounds          GEOMETRY(Polygon, 4326) NOT NULL,
    resolution_m    NUMERIC,
    tile_id         TEXT,               -- source tile identifier
    dataset_version TEXT,
    created_at      TIMESTAMPTZ DEFAULT now()
);
CREATE INDEX raster_tiles_bounds_idx ON raster_tiles USING GIST (bounds);
CREATE INDEX raster_tiles_dataset_idx ON raster_tiles (dataset);

-- Cache for expensive spatial queries (demographics, flood zone, etc.)
-- Keys are dataset-specific, NOT flat proximity hashes.
CREATE TABLE query_cache (
    cache_key        TEXT PRIMARY KEY,  -- compound: dataset:version:entity_id
    dataset          TEXT NOT NULL,
    input_lat        NUMERIC,
    input_lon        NUMERIC,
    result_json      JSONB,
    cached_at        TIMESTAMPTZ DEFAULT now(),
    expires_at       TIMESTAMPTZ         -- NULL = no expiry (use for stable data)
);
CREATE INDEX query_cache_expires_idx ON query_cache (expires_at)
    WHERE expires_at IS NOT NULL;

-- Archived reports (required per §11.8)
CREATE TABLE report_archive (
    report_id            TEXT PRIMARY KEY,
    generated_at         TIMESTAMPTZ DEFAULT now(),
    input_address        TEXT,
    lat                  NUMERIC,
    lon                  NUMERIC,
    source_versions_json JSONB,         -- snapshot of data_source_registry
    pdf_storage_key      TEXT            -- MinIO key
);
```

### 1.2 Deliverables — Phase 1

- [ ] Migration file `001_foundation.sql` — all tables above
- [ ] Migration runner in `plinth-cli db migrate`
- [ ] `plinth-cli db status` prints `data_source_registry` contents

---

## Phase 2 — Vector Data Ingestion (Regional)

Each ingestor follows the `BaseIngestor` contract:
1. **Download** — idempotent fetch to local staging dir
2. **Validate** — check file integrity, record count sanity
3. **Load** — upsert into PostGIS (never truncate/reload blindly)
4. **Register** — upsert row in `data_source_registry`

Start with northeastern Oklahoma bounding box. Each ingestor accepts a `--region` flag or reads `REGION_BBOX_*` from env. Expanding to state or national is a config change, not a code change.

### 2.1 Census TIGER — Block Groups

| | |
|---|---|
| **Source** | Census TIGER/Line block groups |
| **Access** | Download state shapefile |
| **PostGIS table** | `census_block_groups` — geometry + GEOIDs + state/county FIPS |
| **Index** | GIST on geometry |
| **Refresh** | Annual (on ACS release) |
| **Regional clip** | Filter by state FIPS (Oklahoma = 40) initially |

> ACS demographic values (income, age, population, etc.) are fetched via Census API at query time and cached — they are not bulk-loaded. The TIGER geometries are what we store.

### 2.2 FEMA NFHL — Flood Zones

| | |
|---|---|
| **Source** | FEMA National Flood Hazard Layer (~3,200 county GDBs) |
| **Access** | Download county GDB for each target county |
| **PostGIS tables** | `fema_flood_zones` (FLD_HAZ_AR layer) · `fema_unmapped_areas` (derived — areas with no FEMA coverage) |
| **Key fields** | `fld_zone`, `zone_subty`, `sfha_tf`, `bfe_revert`, `static_bfe`, snapshot date |
| **Critical requirement** | Build `fema_unmapped_areas` layer from county boundary minus `fld_zone` polygons. A site with no FEMA data must **never** silently appear as Zone X. |
| **Index** | GIST on geometry |
| **Refresh** | Monthly — compare FEMA NFHL last-modified header against local snapshot |

> County GDB schema varies across releases. Document the field mapping in `data_source_registry.notes`.

### 2.3 IECC Climate Zone Boundaries

| | |
|---|---|
| **Source** | DOE/PNNL IECC 2021 Climate Zone vector file |
| **Access** | Single national download |
| **PostGIS table** | `iecc_climate_zones` — `zone_label` (e.g., "3A"), `zone_description`, geometry |
| **Refresh** | Per code cycle (effectively never during POC) |

### 2.4 FEMA National Risk Index (NRI)

| | |
|---|---|
| **Source** | FEMA NRI tabular CSV — census tract level |
| **Access** | Single national download |
| **PostGIS table** | `fema_nri` — joined to TIGER tract geometry by GEOID |
| **Key fields** | `risk_score`, `risk_ratng`, component scores (flood, wind, earthquake, wildfire, etc.) |
| **Attribution** | Must be labeled "Source: FEMA National Risk Index" in every report output |
| **Refresh** | Annual |

### 2.5 NHDPlus High Resolution — Hydrography

| | |
|---|---|
| **Source** | USGS NHDPlus HR (Vector Processing Unit downloads) |
| **Access** | VPU-by-VPU download (VPU 11 covers Arkansas-White-Red, which includes NE Oklahoma) |
| **PostGIS tables** | `nhd_flowlines` · `nhd_waterbodies` — filtered to named features |
| **Refresh** | Annual check |

### 2.6 USDA SSURGO — Soil

| | |
|---|---|
| **Source** | USDA SSURGO state .gdb files — **download, not SDA API** (API has 5–30s latency, frequent downtime) |
| **Access** | Oklahoma .gdb from Web Soil Survey |
| **PostGIS tables** | `ssurgo_mapunits` (geometry) · `ssurgo_muaggatt` (map unit aggregated attributes) · `ssurgo_component` (component-level data) |
| **Key fields** | `musym`, `muname`, `hydgrpdcd` (hydrologic soil group), `drclassdcd` (drainage class), `slopegraddcp`, `taxclname` |
| **Output constraint** | Raw USDA taxonomy only — no derived suitability or foundation ratings |
| **Refresh** | Annual |

### 2.7 Deliverables — Phase 2

- [ ] `BaseIngestor` class with download/validate/load/register contract
- [ ] Ingestor for each of the 6 sources above
- [ ] Each ingestor idempotent: re-running doesn't duplicate data
- [ ] Each ingestor updates `data_source_registry` on completion
- [ ] `plinth-cli ingest all --region ne-oklahoma` runs all 6 in dependency order
- [ ] PostGIS has spatial indexes and basic query sanity checks pass

---

## Phase 3 — Raster Data Ingestion

Rasters are stored as Cloud-Optimized GeoTIFFs (COGs) in MinIO. PostGIS holds only the tile index (`raster_tiles`). Queries clip the bounding box from the COG at query time using GDAL/rasterio.

**COG pipeline per dataset:**
1. Download source raster (national or regional)
2. Reproject to EPSG:4326 if needed
3. Convert to COG (`gdal_translate` with tiling + overviews)
4. Upload to MinIO with structured key: `{dataset}/{version}/{tile_id}.tif`
5. Insert row into `raster_tiles`

### 3.1 USGS 3DEP — Digital Elevation Model (1/3 arc-sec, ~10m)

| | |
|---|---|
| **Source** | USGS National Map — 1/3 arc-second DEM tiles |
| **Tile count (regional)** | ~10–20 tiles for NE Oklahoma |
| **Tile count (national)** | ~3,500 |
| **Storage per tile** | ~50–150 MB as COG |
| **Derived products** | Slope raster — computed at query time via `gdal.DEMProcessing` with edge buffering (see §5.3 note in requirements) |
| **MinIO key pattern** | `usgs-3dep/2023/{tile_id}.tif` |
| **Refresh** | Annual check |

> **Slope computation note:** Clip DEM with at least 1 grid cell (~10m) buffer beyond the AOI *before* computing slope. Clipping exactly to AOI produces incorrect edge values.

### 3.2 NLCD — National Land Cover Database

| | |
|---|---|
| **Source** | MRLC NLCD (most recent release) |
| **Size** | 10–15 GB national; clip to region for POC |
| **COG strategy** | Clip to state/region extent, convert to COG, upload to MinIO as single tile |
| **Key fields (categorical)** | NLCD class code → class name lookup table in Postgres |
| **Refresh** | Annual check |

### 3.3 USGS National Seismic Hazard Maps — PGA

| | |
|---|---|
| **Source** | USGS NSHM — Peak Ground Acceleration raster (2% in 50 years) |
| **Format** | GeoTIFF — national |
| **COG strategy** | Convert full national raster to COG (small enough to keep as single file, ~200 MB) |
| **Output constraint** | Report raw PGA value only. No Seismic Design Category derivation. |
| **Refresh** | Annual check (~5-year major cycles) |

### 3.4 USDA Wildfire Hazard Potential

| | |
|---|---|
| **Source** | USDA Forest Service WHP (confirm: WHP product, not WFHP — names vary) |
| **Format** | GeoTIFF raster — national |
| **COG strategy** | Single national COG |
| **Output** | WHP percentile vs. national + WUI classification |
| **Citation** | Source, version, methodology, and data vintage explicitly cited in every report |
| **Refresh** | Annual (at dataset release) |

### 3.5 Deliverables — Phase 3

- [ ] `cog.py` — COG conversion helper (wraps `gdal_translate`)
- [ ] `clip.py` — Bounding-box clip with configurable buffer
- [ ] `minio.py` — Upload/download/presign helpers
- [ ] Ingestor for each of the 4 rasters above
- [ ] `raster_tiles` populated for all loaded tiles
- [ ] `plinth-cli raster index` rebuilds tile index from MinIO bucket listing
- [ ] Query test: given a lat/lon, find the correct tile and extract a value

---

## Phase 4 — API-Based Sources (Cached)

These sources are fetched at query time (not bulk-loaded). Results are cached in `query_cache` using dataset-specific keys.

### 4.1 NASA POWER — Climate & Solar

| | |
|---|---|
| **API** | `https://power.larc.nasa.gov/api/temporal/climatology/point` |
| **Parameters** | Temperature, precipitation, humidity, solar irradiance, degree days |
| **Grid resolution** | ~0.5° (~50 km) — cache by POWER grid cell ID + parameter set |
| **Cache key** | `nasa-power:{grid_cell_id}:{param_hash}:{date_range}` |
| **Cache TTL** | 30 days |
| **Failure behavior** | Inline flag: "Climate data unavailable as of [date]" — never silent blank |
| **Implementation** | Exponential backoff + retry. Check cache before any API call. |

### 4.2 EPA AQS — Air Quality

| | |
|---|---|
| **API** | `https://aqs.epa.gov/aqsweb/documents/data_api.html` |
| **Outputs** | Nearest monitor: PM2.5 annual mean (µg/m³), ozone design value (ppb), monitor name, distance |
| **Cache key** | `epa-aqs:{county_fips}:{year}` |
| **Cache TTL** | 365 days (annual summary data changes once/year) |
| **Flag** | If no monitor within 50 miles: inline flag required |
| **Note** | AQS data lags 6–18 months. Do not present as real-time AQI. |

### 4.3 USGS Earthquake Catalog

| | |
|---|---|
| **API** | `https://earthquake.usgs.gov/fdsnws/event/1/` |
| **Query** | M3+ events within 50 mi of site, last 50 years |
| **Outputs** | Event count + max magnitude (within defined time window — state explicitly in report) |
| **Cache key** | `usgs-eq:{grid_cell_100km}:{start_year}:{mag_threshold}` |
| **Cache TTL** | 30 days |
| **Rate limit** | 20,000 req/day — well within budget at POC scale |

### 4.4 NOAA Climate Normals (1991–2020)

| | |
|---|---|
| **Source** | NCEI Climate Normals — download tabular CSV (not API) |
| **PostGIS table** | `noaa_climate_normals` — station ID, lat/lon, monthly normals |
| **Geometry** | Point geometry on station location (for nearest-station spatial queries) |
| **Flag** | If nearest station > 15 miles: inline flag required |
| **Refresh** | Decadal |

### 4.5 FCC Broadband

| | |
|---|---|
| **API** | FCC National Broadband Map API |
| **Outputs** | Service availability, max advertised download/upload tier |
| **Cache key** | `fcc-broadband:{location_id}` (FCC Location Fabric ID) |
| **Cache TTL** | 90 days |
| **Mandatory disclaimer** | "FCC data is self-reported by ISPs and may significantly overstate actual coverage." — must appear inline in every report. |

### 4.6 Deliverables — Phase 4

- [ ] API client module for each of the 5 sources above
- [ ] `query_cache` read/write wrappers with TTL enforcement
- [ ] Cache invalidation CLI: `plinth-cli cache clear --dataset nasa-power`
- [ ] Each client has defined failure behavior (flag, not error)

---

## Phase 5 — Prefect Flows (Scheduled Refresh)

### 5.1 Flow Design

| Flow | Schedule | Trigger |
|---|---|---|
| `ingest-all-regional` | On-demand | Initial setup and teardown/rebuild |
| `refresh-fema-nfhl` | Monthly | Compare FEMA last-modified header vs. local snapshot |
| `refresh-annual` | Annual (Jan 1) | Census ACS, SSURGO, NLCD, USDA WHP, NOAA Normals |
| `check-staleness` | Weekly | Alerts if any source exceeds `next_review_date` in registry |
| `cache-expire` | Daily | Purges expired rows from `query_cache` |

### 5.2 FEMA Refresh Logic

```
1. HEAD request to FEMA NFHL endpoint
2. Compare Last-Modified against data_source_registry.last_downloaded
3. If delta detected:
   a. Download updated county GDB(s)
   b. Validate schema
   c. Upsert into fema_flood_zones (replace by county)
   d. Rebuild fema_unmapped_areas for affected counties
   e. Clear query_cache rows where dataset = 'fema-nfhl'
   f. Update data_source_registry
4. Emit structured log with outcome
```

### 5.3 Idempotency & Repeatability

Every flow must be safe to re-run:
- Downloads use content-hash or ETag checks — don't re-download unchanged files
- DB loads use `INSERT ... ON CONFLICT DO UPDATE` (upsert), never `TRUNCATE + INSERT`
- MinIO uploads use `--if-not-exists` semantics (check before upload)
- `bootstrap.sh` + `plinth-cli ingest all` = full rebuild from scratch in one command

### 5.4 Deliverables — Phase 5

- [ ] All 5 flows implemented and deployed to Prefect worker
- [ ] Flows visible and triggerable from Prefect UI
- [ ] `check-staleness` flow sends structured log (Sentry or stdout for POC)
- [ ] Full teardown + rebuild test: `docker-compose down -v && ./bootstrap.sh` produces a healthy system

---

## Phase 6 — Query Layer (Spatial Lookups)

Before report generation, validate that point-in-polygon and nearest-feature queries work correctly.

### 6.1 Core Query Functions

Implement and test each as a standalone Python function with input `(lat, lon)`:

| Function | Description |
|---|---|
| `query_flood_zone(lat, lon)` | Point-in-polygon against `fema_flood_zones`. Returns zone, subtype, BFE, snapshot date. Falls back to `fema_unmapped_areas` check. |
| `query_fema_nri(lat, lon)` | Find Census tract containing point → join to `fema_nri` |
| `query_iecc_zone(lat, lon)` | Point-in-polygon → climate zone label + description |
| `query_census_block_groups(lat, lon, radii_mi)` | Area-weighted intersection for 1/5/10-mi buffers. Uses PostGIS `geography` type. |
| `query_elevation(lat, lon)` | Find tile in `raster_tiles` → clip COG from MinIO → extract value |
| `query_slope(lat, lon, radius_m)` | Clip buffered DEM → run `DEMProcessing` → extract mean/max |
| `query_land_cover(lat, lon, radius_m)` | Clip NLCD → mode classification + impervious % |
| `query_soil(lat, lon)` | Point-in-polygon `ssurgo_mapunits` → join to `muaggatt` + `component` |
| `query_hydro(lat, lon, radius_mi)` | Nearest named waterbody/flowline from NHD tables |
| `query_seismic_pga(lat, lon)` | Find tile → extract PGA value |
| `query_wildfire_whp(lat, lon)` | Find tile → extract WHP value → compute national percentile |
| `query_noaa_normals(lat, lon)` | Nearest station within 50 mi — flag if >15 mi |
| `query_nasa_power(lat, lon)` | Cache check → API call → cache store |
| `query_epa_aqs(lat, lon)` | Nearest monitor within 50 mi → PM2.5 + ozone |
| `query_earthquakes(lat, lon)` | M3+, 50 mi, 50 yr → count + max magnitude |
| `query_fcc_broadband(lat, lon)` | FCC Location Fabric lookup → tier |

### 6.2 Validation Test Suite

For a known test coordinate in NE Oklahoma:
- Flood zone matches FIRMette
- IECC zone matches DOE lookup
- Soil series name matches USDA Web Soil Survey
- Elevation matches USGS National Map
- Climate normals match NOAA station data

### 6.3 Deliverables — Phase 6

- [ ] All 16 query functions implemented
- [ ] Unit tests with mocked DB/API for each function
- [ ] Integration tests with real data for the NE Oklahoma test coordinate
- [ ] `plinth-cli query all --lat 36.15 --lon -95.99` prints a JSON summary of all data for a coordinate

---

## Phase 7 — Report Generation (Incremental)

Report generation is intentionally staged. Build and validate each section before adding the next. Each step produces a renderable PDF.

**PDF stack:** WeasyPrint + Jinja2. Maps rendered as PNGs via Mapbox Static Images API. Charts via matplotlib (saved as PNG). All embedded in HTML template → PDF.

> **Per requirements §5.2:** Prototype the full PDF layout (all 8 sections, realistic data) before wiring live data to each section. WeasyPrint layout issues compound in long documents.

### Step 7.1 — PDF Skeleton (All Sections, Mocked Data)

Build the full 8-section HTML/CSS template with hardcoded placeholder data. Validate layout, typography, page breaks, map placeholders, and disclaimer placement.

Checklist:
- [ ] Cover page: address, coordinates, report date, report ID, "Prepared for", prominent disclaimer block, QR code placeholder
- [ ] Section 1: Executive Summary + Risk Summary Table (tabular, no color coding)
- [ ] Sections 2–7: All section headers, table layouts, map image placeholders
- [ ] Section 8: Methodology / Sources table
- [ ] Footer: subtle persistent `Plinth · site-intelligence.com · Report ID: [XXXXX]`
- [ ] Appendix page: "About this report" + soft CTA
- [ ] Map attribution: "© Mapbox © OpenStreetMap contributors" on every map

### Step 7.2 — Wire Flood Risk (Section 2 partial)

Connect `query_flood_zone()` to the template. Validate:
- Zone X (mapped low risk) vs. Zone AE (high risk) vs. unmapped display correctly
- NFHL snapshot date appears
- Inline disclaimer adjacent to flood zone field
- Flag language per requirements (data + direction, no insurance conclusions)

### Step 7.3 — Wire Demographics (Section 6)

Connect `query_census_block_groups()` + Census ACS API. Validate:
- Three radius buffers labeled "straight-line radius"
- ACS vintage year displayed
- Area-weighted intersection math checked against manual calc

### Step 7.4 — Wire Physical Context (Section 3)

Connect elevation, slope, land cover, hydrography queries. Validate slope disclaimer at >15%.

### Step 7.5 — Wire Climate & Solar (Section 4 + 5)

Connect NASA POWER + `astral` solar calculations + NOAA Normals. Validate station distance flag.

### Step 7.6 — Wire Environmental Risk: Soil, Seismic, Wildfire, Air Quality (Section 2 remainder)

Connect SSURGO, USGS PGA, WHP, EPA AQS. Validate all inline disclaimers.

### Step 7.7 — Wire FEMA NRI + Infrastructure (Section 2 + 7)

Connect FEMA NRI (with explicit attribution) + FCC Broadband (with mandatory disclaimer).

### Step 7.8 — Wire Mapbox Static Maps

Replace map placeholders with real Mapbox Static Images API calls. Validate attribution on every map.

### Step 7.9 — Executive Summary (Section 1)

Assemble Risk Summary Table from all live data. Wire population snapshot, IECC zone, key data flags.

### Step 7.10 — Methodology Section (Section 8)

Populate from `data_source_registry` snapshot at generation time. Assign unique `report_id`. Write to `report_archive`.

### Step 7.11 — End-to-End Report Generation

`plinth-cli report generate --address "123 Main St, Tulsa, OK"`:
1. Geocode via Mapbox
2. Run all 16 query functions
3. Render WeasyPrint PDF
4. Save to MinIO
5. Write `report_archive` row

---

## Phase 8 — Web Portal (Out of Scope for Data Platform POC)

Placeholder for future: address input form, Mapbox confirmation map, Stripe payment, report delivery. Covered in a separate plan.

---

## Data Freshness & TTL Reference

| Source | Refresh Frequency | Cache TTL | Notes |
|---|---|---|---|
| FEMA NFHL | Monthly check | Short / none | LOMR risk — see §7.2 |
| Census ACS | Annual | 365 days | |
| USGS 3DEP | Annual check | Stable | |
| NLCD | Annual check | Stable | |
| SSURGO | Annual | Stable | |
| USGS Seismic | Annual check | Stable | |
| USDA WHP | Annual | Stable | |
| NASA POWER | Continuous | 30 days | Grid-cell cache |
| NOAA Normals | Decadal | Stable | |
| EPA AQS | Annual | 365 days | |
| FCC Broadband | Periodic | 90 days | |
| USGS Earthquakes | Continuous | 30 days | |

---

## Open Questions (Decisions Deferred)

1. **Mapbox token for POC** — Do you have one, or should we set up a new account? Free tier is 100k static image requests/month — more than enough for POC.
2. **Census API key** — Free, but required. Do you have one?
3. **EPA AQS API key** — Free, required for AQS data access.
4. **Test address** — Confirming NE Oklahoma test coordinate, e.g., `36.15, -95.99` (near Tulsa). Or pick a specific address.
5. **Code repository** — GitHub repo (new or existing)? Or local git only for POC?
6. **Report ID format** — Suggest `PLN-{YYYYMMDD}-{UUID4[:8].upper()}` e.g., `PLN-20260301-A3F9B2C1`. Confirm or adjust.
